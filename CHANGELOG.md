# Changelog

## [Unreleased]

## [v0.2.10] - 2026-05-16

### 优化
- 页面说明入口与页头结构进一步统一：说明按钮固定在标题后方动作区首位，减少被业务按钮顺序影响。
- 硬链接新建/编辑页页头结构统一为 `page-head + page-actions`，提升移动端与后续维护稳定性。
- 日志与映射页页头关键口令输入在窄屏下布局优化，避免输入框与按钮互相挤压。

### 修复
- 修复日志与执行历史页面“长消息展开/收起”在部分场景点击无响应的问题（改为稳定的事件绑定，支持局部刷新后重绑）。

## [v0.2.9] - 2026-05-15

### 优化
- 日志与执行历史页面统一排版：顶部操作区紧凑化、筛选区重排、执行历史分页化。
- 文件映射与缓存管理页面统一排版：视图切换/筛选/主内容区结构一致，提升小屏可读性。
- 多页面说明区统一为可折叠“页面说明”，默认收起，减少首屏占用。
- 硬链接任务新建/编辑页面优化：入库行为并入基础设置，返回任务列表入口增强。

### 新增
- 日志页与映射/缓存页新增每页条数选项：`10/20/50/100`，默认 `20`。

### 修复
- 补齐日志类型中文映射，避免筛选类型出现未归类英文键。

## [v0.2.8] - 2026-05-15

### 优化
- 系统设置页面重构为“系统设置 / 开发与更新”双页结构，降低单页复杂度。
- 系统设置各分区支持独立保存，减少滚动和误操作。
- 映射与缓存页面新增顶部视图切换（全部/仅映射/仅缓存），改善小屏阅读体验。
- 日志与执行历史页面优化：类型中文化补齐、筛选增强、长消息展开、耗时显示优化。

### 调整
- 开发模式改为“保存后手动重启容器生效”，禁用应用内自动重启入口。

### 修复
- 修复开发模式配置保存后应用内重启导致容器退出但未自动拉起的风险。
- 修复版本元信息不一致：Docker 镜像标签与项目版本统一为 `v0.2.8`。

## [v0.2.7] - 2026-05-12

### 优化
- 设置页开发模式支持分区按钮：新增“保存开发模式设置”“保存并重启应用”，并保留全局保存入口。
- 开发模式重启流程改进：`/settings/dev-restart` 在触发重启前先保存当前表单配置，避免“已改未生效”。
- 日志页面类型筛选与列表显示改为中文标签，普通用户更易理解。
- 映射/定时/诊断/设置相关页面术语统一：用户侧“回填”统一为“自动关联”。

### 修复
- 修复开发模式仓库地址空值场景：页面展示与写入运行配置均提供默认仓库兜底。
- 修复部分页面说明文案中中英混用与语义不一致问题（仅改显示层，不改数据库字段/任务键值）。

## [v0.2.6] - 2026-05-12

### 新增
- 开发模式支持“页面配置优先”运行：点击“重启并应用”后写入 `instance/dev_runtime.env`，启动时优先加载该配置。
- 设置页新增开发模式状态接口 `/settings/dev-apply-status` 与 15 秒自动刷新展示。

### 安全
- 开发仓库拉取不再使用 `https://TOKEN@...` 拼接，改为 `Authorization` header 方式，降低 token 暴露风险。
- `.gitignore` 新增 `instance/*` 忽略规则，避免本地数据库与运行标记误提交。

### 优化
- qB 连接层重构：新增会话复用与失效自动重登，减少重复登录与连接开销。
- 映射回填性能优化：候选文件列表预取支持受控并发，降低逐条串行请求延迟。
- 映射回填准确率优化：新增候选评分排序（路径命中优先，其次名称命中），并支持路径归一化映射。
- 回填任务资源控制：批次大小/候选上限/文件列表并发/失败阈值均支持配置，并加入批次自动调优。

### 新增
- 新增回填相关配置项：`backfill_batch_limit`、`backfill_max_candidates`、`backfill_file_fetch_workers`、`backfill_max_failures`、`backfill_path_mappings`。
- 下载器配置新增 `会话复用时长(秒)`（`session_ttl_seconds`），支持按下载器单独控制 qB 会话复用时长。
- 新增回填指标日志 `backfill_metrics`（行数、命中/冲突/跳过、候选均值、filelist 请求与缓存命中）。

### Internal
- 数据库迁移升级至 `v8`，新增复合索引：
  - `idx_file_link_map_backfill_lookup (torrent_hash, deleted_at, backfill_fail_count, downloader_id)`
  - `idx_file_link_map_source_state (source_type, deleted_at, last_seen_at)`
- 回归测试通过：`tests/test_backfill_service.py`、`tests/test_source_type_guard.py`、`tests/test_routes_smoke.py`（16/16）。

## [v0.2.5] - 2026-05-08

### 优化
- 全站列表与弹窗表单继续统一：强化表格分界、统一筛选区按钮与页面分区视觉。
- 硬链接任务新建/修改弹窗优化：`创建目录`与`启用缓存`并排展示，示例说明收敛到提示图标，减少表单占用空间。
- 映射与缓存页搜索提示文案优化为“搜索路径 / 文件关键字 / hash”，与实际筛选能力保持一致。

### 修复
- 修复疑似误删处理列表模板结构细节，避免局部刷新后出现潜在排版错乱。

## [v0.2.4] - 2026-05-07

### 新增
- 开发模式新增代理支持：`APP_DEV_PROXY_URL` / `APP_DEV_NO_PROXY`，用于容器内 Git 拉取与依赖同步。

### 优化
- 硬链接任务页表格重新收敛列宽，修复操作列按钮溢出与必须横向拖动才能完整操作的问题。
- 硬链接任务新建/修改弹窗优化分组结构：新增“删除监控范围”“执行与通知选项”标题，复选项并排布局统一。
- 文件映射页“种子 Hash”列扩宽并增强显示策略，减少被截断带来的辨识困难。
- 系统设置页“配置导入/导出”按钮区改为并排一致布局，修复错版。

### 修复
- 修复多处页面在常见分辨率下列宽、换行和按钮高度不协调导致的视觉错位问题。

## [v0.2.3] - 2026-05-07

### 优化
- 全站 UI 可读性与排版优化：提升页面内容区字体、统一任务页按钮布局、修复弹窗选项对齐体验。
- 映射与缓存表格交互优化：操作列可见性增强，降低横向拖动成本。
- 文档清理与同步：README 去除冗余和过时说明，统一当前登录机制与部署要点。

### 修复
- 修复部分页面在常见分辨率下文字偏小、操作密集难点按的问题。

## [v0.2.2] - 2026-05-07

### 新增
- 登录由弹窗认证改为独立登录页，支持“记住我”持久会话，并补充环境变量凭据说明。
- 多页面补充信息提示图标与悬浮解释，降低配置理解门槛。

### 优化
- 前端样式统一为本地静态资源（Bootstrap/Font Awesome 本地化），减少外网依赖风险。
- 统一页面标题、表格与操作区样式，改善不同页面字号/对齐不一致问题。

### 修复
- 修复会话认证辅助函数缺失导致的登录与受保护路由 500 错误。
- 修复设置页相关测试在 CSRF 会话初始化流程下的提交失败问题。

## [v0.2.1] - 2026-05-07

### 新增
- 仪表盘新增“待判定来源健康”卡片，展示待判定数量、阈值状态与最近事件时间，并提供日志/诊断快捷入口。
- 新增待判定来源判定与路由烟雾测试，覆盖窗口期判定和关键页面可用性。

### 优化
- 删除联动中的 `delete_pending_source` 日志由逐条记录改为按轮聚合，降低日志噪音并保留样例信息。
- 系统诊断新增待判定保护配置展示、待判定映射数量检查与最近待判定事件列表。
- 设置项保存/导入统一使用同一键集合，减少配置项新增时遗漏风险。

### 修复
- 修复清缓存后映射重建窗口期来源误判问题：新映射默认 `pending`，删除联动对待判定来源采用保守策略，避免误触发自动删种。
- 修复待判定保护配置项在设置页保存/导入时未持久化的问题。

## [v0.2.0] - 2026-05-07

### Changed
- 稳定版体验优化：统一定时任务页面文案与视觉层级，任务类型改为中文可读标签，并为高风险任务增加显著标识。
- 前端表格可读性增强：表头吸顶、行间对比、状态信息更清晰。
- Cron 校验增强：不再仅检查“5段”，新增基础合法性范围校验（分/时/日/月/周）。

### Fixed
- 降低隐性配置错误风险：非法 Cron 输入会在提交时被更准确拦截。

## [v0.1.9] - 2026-05-07

### Fixed
- 修复来源判定隐患：不再将 `source_type=manual` 且存在历史哈希/下载器字段的记录强制判为下载器来源。
- 删除联动来源兜底收敛：仅在 `source_type` 缺失/异常时，才依据 `torrent_hash + downloader_id` 推断为下载器来源，避免误触发删种。

## [v0.1.8] - 2026-05-06

### Changed
- 删除联动阈值策略由“超阈值整轮阻断”改为“按阈值分批执行”：每轮最多处理 `单次最大删除` 条，剩余记录下轮继续。
- 联动结果文案增强：返回信息显示“本轮处理数量/总命中数量”，便于判断是否还有待处理项。

### Fixed
- 修复大批量删除场景下可能长期卡在阈值阻断、无法推进的问题。

## [v0.1.7] - 2026-05-05

### Changed
- 系统定时任务增强说明：在定时任务页明确标注各系统任务用途，降低误操作风险。
- “系统缓存清理”任务改为默认禁用，并补充高风险提示文案（建议仅排障时手动执行）。
- 历史任务兼容处理：启动时自动将“系统缓存清理”调整为禁用并同步说明。

## [v0.1.6] - 2026-05-05

### Changed
- 发布版本号升级至 `v0.1.6`，用于后续问题修复与分发区分。

## [v0.1.5] - 2026-05-05

### Fixed
- 修复“先删除硬链接文件后不触发联动删源/删种”的问题：移除联动流程中的提前时间戳写入，避免被冷却窗口误判跳过。
- 修复删除后目录残留问题：删除对侧文件后会尝试按任务根目录边界清理空目录。

### Changed
- 删除联动来源判定增强：历史记录若存在 `torrent_hash/downloader_id`，即使 `source_type` 旧值为 `manual` 也按下载器来源处理。
- 侧边栏与页面标题文案统一为“疑似误删处理/疑似误删处理中心”，降低入口歧义。

## [v0.1.4] - 2026-05-05

### Added
- 硬链接任务新增内建删除联动配置（监控源删/目标删、下载器、通知器、冷却、阈值、试运行、通知开关）。
- 启动新增“版本首次运行自动备份旧库再迁移”机制：首次进入新版本且存在结构升级时自动备份到 `backup_dir/migration-pre`。

### Changed
- 执行链改为“先删除联动扫描，再硬链接扫描”，避免已删除文件被再次硬链接回来。
- 删除联动页面收敛为“疑似误删处理中心”，新增任务入口迁移至硬链接任务。
- 定时任务创建/编辑收敛：默认不再新增独立删除扫描任务，重点围绕硬链接任务调度。

### Internal
- 数据库结构目标版本升级为 `v7`，新增硬链接任务删除联动字段。
- `schema_meta` 新增版本迁移状态键，支持一次性升级前备份和状态追踪。

## [v0.1.3] - 2026-05-05

### Fixed
- 修复映射页“重试关联”高负载问题：由按下载器批量回填改为单条映射定向回填，显著降低 qB CPU 压力。
- 修复映射页部分操作的整页刷新与筛选状态丢失问题：统一为 AJAX 局部刷新 `mappingPanel`。
- 修复映射页批量操作提交逻辑：改用 `requestSubmit` 触发，避免绕过前端事件绑定导致交互失效。

### Changed
- 映射页结构拆分为 `templates/_mapping_panel.html` 局部模板，前后端目标面板统一，交互更稳定。
- 映射页局部刷新后自动重绑复选框与计数提示逻辑，避免“刷新后按钮无反馈”。

- 定时任务编辑能力增强：支持在“修改定时任务”中同步修改任务类型与关联目标任务，不再仅限名称和 Cron。
- 全局操作提示优化为右下角悬浮通知：默认显示 5 秒、支持手动关闭，避免页面顶部提示导致强制滚动和可读性差。
### Docs
- 同步更新 `README.md`、`Dockerfile`、`VERSION` 与本更新日志，确保文档与程序行为一致。

## [v0.1.2] - 2026-05-05

### Changed
- 系统映射回填默认计划从每 30 分钟调整为每 60 分钟（`0 * * * *`），并对历史默认计划自动迁移。
- GitHub 版本检查缓存默认值调整为 720 分钟，减少不必要的外部请求。

### Added
- 仪表盘“最近执行”新增操作：已完成任务可一键重试，运行中任务可发送停止请求。
- 系统诊断页新增“当前运行中的任务”面板，展示已运行时长并支持手动停止。
- 映射回填新增失败计数：同一记录连续超过 2 次未匹配后自动跳过，避免重复消耗资源。

### Fixed
- 优化回填性能：引入回填过程内种子文件列表缓存与精确匹配缓存，降低对 qB 文件列表接口的重复请求。
- 优化“任务正在执行中”提示，展示已运行秒数，减少用户困惑。

### Internal
- 数据库迁移升级到 `v6`，新增回填失败计数字段与索引（`backfill_fail_count`、`backfill_last_attempt_at`）。

## [v0.1.1] - 2026-05-05

### Added
- 映射与缓存页面新增批量操作：支持勾选、全选、取消全选、反选后批量删除。
- 映射页面新增“清空全部映射”能力（仅删除数据库映射，不删除真实文件，支持关键口令保护）。

### Fixed
- 修复回填候选为空时直接跳过的问题：当种子名/路径启发式未命中时，自动回退到种子文件列表精确匹配。
- 回填日志增强：新增 `candidate_empty_fallback_matched` 与 `candidate_empty_fallback_no_match`，便于定位匹配行为。
- 修复映射页批量操作实现中的前端结构风险，避免嵌套表单导致的潜在提交异常。

## [v0.1.0] - 2026-05-05

### Fixed
- 修复 AJAX 表单提交在特定场景下 URL 被污染的问题：不再直接依赖 `form.action`，避免出现 `/[object RadioNodeList]` 导致的 404。
- 修复筛选表单同类隐患：筛选请求也改为安全读取 action 属性，避免后续页面复发。
- 优化前端错误提示：当接口返回非 JSON 响应时，页面将显示更明确的失败信息，便于定位问题。

### Security
- 依赖安全加固：`cryptography` 升级到 `48.0.0`。
- 依赖漏洞复检通过：`pip-audit` 未发现已知漏洞。

### Changed
- 版本提升为 `v0.1.0`，作为前端交互稳定性与依赖安全修复的里程碑版本。

## [v0.0.13] - 2026-05-05

### Fixed
- 修复设置页保存配置时偶发请求地址异常：前端提交逻辑不再直接依赖 `form.action`，避免被同名表单字段污染后出现 `/[object RadioNodeList]` 导致 404。
- 修复设置页“保存/检查更新”按钮命名冲突：将按钮字段从 `action` 调整为 `submit_action`，消除与表单 action 语义冲突。
- 优化 AJAX 异常提示：当服务端返回非 JSON 响应时，页面会显示明确状态信息，不再统一误报“网络错误”。

## [v0.0.12] - 2026-05-05

### Fixed
- 修复群晖容器日志不可见问题：应用启动改为无缓冲输出，并强制保留 stdout 控制台日志链路，容器日志页可直接看到访问/操作/错误日志。
- 修复部分大数据量场景下页面卡顿：日志页与映射页分页粒度优化，降低单次渲染与查询负载。

### Changed
- 性能优化：日志页执行历史列表由 100 条调整为 50 条，映射页与缓存页每页由 100 条调整为 50 条，提升响应速度与交互流畅度。
- 数据库迁移升级到 `v5`：补充日志与映射相关索引（created_at/status/operation_type 等），提高筛选与排序效率。
- Compose 增加 `ACCESS_LOG_ENABLED` 与 `PYTHONUNBUFFERED` 环境项示例，部署行为与运行日志表现更一致。

## [v0.0.11] - 2026-05-05

### Fixed
- 修复编辑弹窗频繁闪动与无法关闭问题：将定时任务等面板中的弹窗结构统一移出表格循环区域，避免无效 DOM 导致的 Bootstrap Modal 状态异常。
- 修复任务编辑交互稳定性：弹窗提交后可正常关闭，不再需要整页刷新。

### Changed
- 模板结构统一重构：`downloader/notifier/delete_monitor/logs/mapping` 页面改为可维护的多行语义化结构，降低后续改动引发回归的概率。
- 新增弹窗命名统一：新增窗口 ID 改为语义化唯一命名（如 `addDownloaderModal`、`addNotifierModal`、`addDeleteMonitorModal`）。
- 日志与映射页面增强空状态提示与排版一致性，长文本展示与页面可读性进一步优化。

## [v0.0.10] - 2026-05-05

### Added
- 设置页、硬链接页、下载器页、通知器页、删除联动页新增信息提示图标与悬浮说明，降低新手理解成本。
- 映射页新增映射记录操作：支持“重试关联”与“删除映射记录”（仅删除数据库映射，不删除真实文件）。
- 长文本自动换行能力增强：路径、Hash、日志消息等超长内容可完整显示，便于排障。

### Changed
- 映射与缓存页面中文化与可读性优化：来源类型改为“手动文件/下载器文件”，并增加关联状态直观展示。
- 设置页重构为分区卡片样式，保存设置与检查更新按钮职责明确且互不影响。
- 代理相关提示文案统一为“留空=使用系统设置中的统一代理”，避免配置歧义。
- 默认统一外网代理地址模板统一为 `http://127.0.0.1:7890`，与设置页示例保持一致。

### Fixed
- 修复待确认筛选值兼容问题：页面显示“映射 hash”，提交值保持 `mapping_hash`，避免筛选失效。
- 修复部分中英文术语混用导致的认知不一致（如 Torrent/Chat ID/Dry Run 等）。
- 修复 README 文案替换遗留问题，确保说明文本与实际配置项一致。

## [v0.0.9] - 2026-05-05

### Added
- 新增来源感知双向联动架构：`FileLinkMap.source_type` 区分 `manual/downloader`，删除联动按来源执行不同策略。
- 设置页新增 6 个联动开关：手动来源与下载器来源分别控制“删源/删目标/删种”。
- 映射页新增来源类型展示与筛选（全部/manual/downloader），便于排查联动行为。
- 新增版本信息能力：`VERSION` 本地版本文件、设置页 GitHub 最新版本检查与手动检查入口（代理统一由设置页配置）。
- 新增应用日志滚动分割：支持按大小分割与保留份数，减少磁盘占用和日志读取卡顿。

### Changed
- 硬链接页与系统设置统一为“模板+任务覆盖”模式：默认扩展名/排除目录仅作为新建任务默认值。
- 代理配置统一：移除分散代理配置项，Telegram 与 GitHub 均使用设置页 `proxy_url`。
- 删除联动核心服务重构：先处理文件对侧联动，再按来源决定是否删种；manual 来源默认不删种。
- 回填逻辑增强：命中下载器后自动将映射来源升级为 `downloader`。
- 启动兼容迁移增强：自动补齐 `file_link_map.source_type` 列并回填历史数据。
- 设置页与后端配置对齐：移除未接线的 `delete_delay_seconds`、`notify_on_hardlink`，避免“可配置但不生效”。
- 自动清理日志开关生效：关闭 `auto_clean_logs` 后，Cron 日志清理将记录跳过而不再执行。

### Fixed
- 修复硬链接任务开关失效：`/hardlink/toggle` 双重取反导致状态不变。
- 移除“重启自动更新”能力，避免不同部署环境下不确定性与误更新风险。
- 修复默认排除目录未生效：任务新增/修改时空值将正确回退到 `default_exclude_dirs`。

## [v0.0.8] - 2026-05-04

### Fixed
- 修复删除联动异常：`DeleteMonitorTask` 补充 `downloader/notifier` 关系，解决 `object has no attribute downloader` 报错。
- 修复删除联动时区异常：统一兼容 naive/aware datetime，解决 `can't subtract offset-naive and offset-aware datetimes`。
- 修复前端交互卡死：AJAX 成功后强制清理 modal 遮罩和 `modal-open` 状态，避免页面“不可点击”。

### Changed
- 增强容器日志可见性：日志强制输出到 stdout，并增加启动日志标记，便于群晖容器日志排查。
- 删除联动目录匹配改为规范路径判断，避免 `/media/a` 误匹配 `/media/ab`。
- 硬链接覆盖策略加固：仅允许覆盖系统托管目标文件，非托管同名文件直接跳过，防止误删。

## [v0.0.7] - 2026-05-04

### Added
- 登录安全限流：同一 IP 在 1 分钟内连续认证失败超过 3 次，将封禁 30 分钟（返回 429 与 Retry-After），重启容器后自动清空封禁状态。
- 删除联动新增“疑似误删待确认队列”：风险匹配（`name_match` / `path_match`）默认转为人工确认，不再直接删种。
- 删除联动页面新增“待确认（疑似误删）”操作区，可手动“确认删除 / 驳回”。
- 系统设置新增 `notify_on_risky_delete` 与 `delete_match_strict_mode` 开关。
- 容器日志增强：新增 `auth_blocked` / `auth_failed_blocked` 记录，便于在群晖容器日志中直接排查暴力尝试与封禁行为。

### Changed
- 时区显示修复：页面时间统一按 `TZ` 渲染（默认 `Asia/Shanghai`），日志页/仪表盘/映射页不再直接显示 UTC。
- 前后端交互优化：核心操作统一 AJAX 反馈，结果在页面顶部即时提示并刷新对应面板。
- 删除任务清理逻辑修复：删除硬链接任务时，缓存清理从“前缀模糊匹配”改为“规范路径匹配”，避免误删相似目录缓存。
- 删除识别逻辑修复：按“实际消失侧（源/目标）”判断删除路径，减少误识别与漏识别。

### Fixed
- 修复定时计划修改体验与说明，新增 Cron 表达式可视化解释。
- 修复下载器测试与多项任务操作反馈不直观的问题，统一反馈与日志落盘。

## [v0.0.4] - 2026-05-04

### Changed
- Compose 默认目录映射方案升级为“单媒体根目录”模式：`MEDIA_ROOT -> /media`，任务可在程序内自由配置子目录。
- `docker-compose.yml` 与 `docker-compose.prod.yml` 增加完整中文注释，并保留双目录映射的可选注释模板。
- `.env.example` 与 `.env.prod.example` 同步为单目录映射示例，显著降低新手配置复杂度。
- `README.md` 重构为小白友好文档：新增可直接复制的 Compose 示例、群晖图形界面部署步骤、任务模板与 FAQ。
- `scripts/bootstrap.sh` 初始化提示文案更新为中文，并明确要求优先配置 `SECRET_KEY` 与 `MEDIA_ROOT`。

### Verified
- 本地 `make check` 全量通过（语法检查 + 路由测试 + 冒烟检查）。
- 依赖漏洞扫描通过：`pip-audit` 未发现已知漏洞。


### Added
- 新增任务执行历史模型 `JobExecutionLog`，记录手动/定时执行状态、耗时与结果消息。
- 新增数据库备份服务 `core/services/backup_service.py`（SQLite 原生 backup API + 保留轮转）。
- 新增删除监控“立即执行”与 Cron 任务“立即执行”入口。
- 新增 Cron 任务类型 `db_backup` 与系统默认备份计划（每 6 小时）。

- Telegram 通知新增代理配置：`tg_proxy_url`（支持 `http://127.0.0.1:7890`）。
- Telegram 通知新增 API Base 配置：`tg_api_base`。
- 新增统一 UI 基座模板 `templates/base.html`。
- 新增最小冒烟测试：`tests/test_routes_smoke.py`。
- 新增路由依赖对象：`core/deps.py`。

### Changed
- UI 统一升级为现代化侧边导航与仪表盘布局，增强移动端适配。
- Compose 持久化补充 `./data/backups:/app/data/backups`，避免备份丢失。
- 设置页新增 `backup_dir` 与 `backup_keep_last` 配置。

- 架构重构为分层：
  - `core/models.py`（模型）
  - `core/extensions.py`（扩展）
  - `core/routes/web.py`、`core/routes/api.py`（路由）
  - `core/services/*.py`（业务服务）
- 业务模式从实时监听切换为定时扫描驱动（硬链接、删除联动、回填任务）。
- 删除联动增加冷却时间、单次阈值、Dry Run 防误删策略。
- Cron 支持映射回填任务 `backfill_mapping`。
- 全量替换 SQLAlchemy `Query.get()` 为 `db.session.get(...)`。

### Removed
- 删除未接入的重复定义与冗余文件。
- 移除未使用依赖：`watchdog`、`python-telegram-bot`。

### Added
- 新增 `docker-compose.yml`（基础部署）与 `.env.example`。
- 新增 `docker-compose.prod.yml`（生产安全加固版）。
- 新增 `.env.prod.example` 生产环境变量模板。
- 新增运维脚本：`scripts/bootstrap.sh`、`scripts/backup.sh`。
- 新增运行数据目录结构：`data/instance/.gitkeep`、`data/backups/.gitkeep`。

### Changed
- README 新增基础与生产 Compose 部署文档、持久化与备份说明。
- `.gitignore` 新增运行数据与 `.env` 忽略规则，防止敏感信息误提交。

### Security
- 升级 `cryptography` 至 `46.0.7`，修复 `CVE-2026-39892`。
- 修复 `allowed_roots` 路径白名单校验边界：从字符串前缀匹配改为规范化路径包含校验，避免路径前缀绕过。
- 启动时增加默认 `SECRET_KEY` 风险告警，降低误用默认密钥风险。
- CSRF 防护保持启用。
- 新增 Dependabot 自动依赖检查（pip + GitHub Actions）。
- 新增 Dependabot 安全自动合并策略（仅 patch/minor）。
