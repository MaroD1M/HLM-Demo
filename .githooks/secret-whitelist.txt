# One regex per line. Lines starting with # are comments.
# Keep patterns specific to avoid suppressing real leaks.
# Example (allow a known placeholder in docs only):
# ^\+.*README\.md.*replace-with-a-strong-random-string
